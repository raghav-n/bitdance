"""Task modules live here.

Add modules like `ingest.py`, `normalize.py`, etc., and decorate task functions with
`@orchestrator.task(name=..., inputs=[...], outputs=[...])`.

Do not implement logic here unless it's shared helpers; keep tasks modular per file.
"""
